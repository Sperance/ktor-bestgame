package application.enums

enum class EnumCharacterSkills(val type: Int) {
    // ======================================================
    // 1 — РЕМЕСЛЕННЫЕ НАВЫКИ (Craft)
    // ======================================================
    LUMBERJACK(1),      // Лесоруб
    STONEMASON(1),      // Каменщик
    ALCHEMY(1),         // Алхимия
    BLACKSMITH(1),      // Кузнечное дело
    BUILDER(1),         // Строительство
    TAILOR(1),          // Портняжное дело
    ENCHANTER(1),       // Зачарование
    JEWELER(1),         // Ювелирное дело
    COOK(1),            // Кулинария
    FISHERMAN(1),       // Рыболовство
    MINER(1),           // Горное дело
    FARMER(1),          // Земледелие
    HERBALIST(1),       // Травничество
    CARPENTER(1),       // Плотничество
    CARTOGRAPHER(1),    // Картография
    HUNTER(1),          // Охотник

    // ======================================================
    // 2 — БОЕВЫЕ НАВЫКИ (Combat)
    // ======================================================
    // ---- Физическое оружие ----
    MELEE_COMBAT(2),        // Общий ближний бой
    SWORDSMANSHIP(2),       // Владение мечом
    AXE_COMBAT(2),          // Владение топором
    POLEARM_COMBAT(2),      // Владение древковым оружием
    UNARMED_COMBAT(2),      // Рукопашный бой
    DUAL_WIELDING(2),       // Два оружия
    TWO_HANDED_WEAPON(2),   // Двуручное оружие

    // ---- Дальний бой ----
    RANGE_COMBAT(2),        // Общий дальний бой
    ARCHERY(2),             // Лук
    CROSSBOW(2),            // Арбалет
    THROWING(2),            // Метательное оружие (копья, ножи)

    // ---- Магия ----
    MAGIC_COMBAT(2),        // Общая магия
    FIRE_MAGIC(2),          // Огненная магия
    WATER_MAGIC(2),         // Водная магия
    EARTH_MAGIC(2),         // Земляная магия
    AIR_MAGIC(2),           // Воздушная магия
    ELECTRIC_MAGIC(2),      // Электрическая магия
    DARK_MAGIC(2),          // Тёмная магия
    LIGHT_MAGIC(2),         // Светлая магия
    CHAOS_MAGIC(2),         // Магия хаоса
    SUMMONING(2),           // Призыв существ

    // ---- Защита ----
    DEFENDER(2),            // Общая защита
    SHIELD_BEARER(2),       // Владение щитом
    ARMOR_MASTERY(2),       // Мастерство ношения брони
    PARRY(2),               // Парирование
    DODGE(2),               // Уклонение

    // ======================================================
    // 3 — СПЕЦИАЛЬНЫЕ НАВЫКИ (Exploration & Special)
    // ======================================================
    EXPLORER(3),            // Исследователь
    STEALTH(3),             // Скрытное передвижение
    LOCKPICKING(3),         // Взлом замков
    TRAP_DISARM(3),         // Обезвреживание ловушек
    CLIMBING(3),            // Лазание
    SWIMMING(3),            // Плавание
    SURVIVAL(3),            // Выживание в дикой природе
    ANIMAL_TAMING(3),       // Приручение животных
    RIDING(3),              // Верховая езда
    SAILING(3),             // Мореплавание
    DIVING(3),              // Глубоководное погружение
    CAVING(3),              // Спелеология

    // ---- Социальные и торговые ----
    PERSUASION(3),          // Убеждение
    INTIMIDATION(3),        // Запугивание
    BARTER(3),              // Торговля
    DISGUISE(3),            // Маскировка
    GAMBLING(3),            // Азартные игры
    LORE(3),                // Эрудиция (история, география, культура)
    MEDICINE(3),            // Медицина и первая помощь
    POISON_CRAFT(3),        // Приготовление ядов
}