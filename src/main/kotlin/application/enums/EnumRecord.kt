package application.enums

/**
 * Рекорды — статистики, которые сохраняют максимальное достигнутое значение.
 * Обновляются только если новое значение > текущего.
 * Код для компактной сериализации: "M1:9999.0"
 */
enum class EnumRecord(val code: String, val text: String) {
    MAX_DAMAGE("M1", "Макс. урон за удар"),
    MAX_CRIT_DAMAGE("M2", "Макс. критический урон"),
    MAX_KILL_STREAK("M3", "Макс. серия убийств"),
    MAX_LEVEL("M4", "Макс. уровень"),
    MAX_GOLD_HELD("M5", "Макс. золота одновременно"),
    HIGHEST_ENHANCE("M6", "Макс. уровень улучшения"),
    FASTEST_BOSS_KILL("M7", "Самое быстрое убийство босса (сек)"),
    HIGHEST_FLOOR("M8", "Самый высокий этаж подземелья"),
}
