package application.enums

/**
 * Редкость монстра - как в PoE: обычный, магический с одним-двумя модификаторами и редкий с
 * тремя-четырьмя. Катает её клиент по весам правила редкости, а сервер по ней умножает добычу и опыт.
 * `UNIQUE` (с 0.32.0) - только босс карты, случайно она не выпадает никогда. С 0.66.0 живёт здесь:
 * на неё ссылается описание модификатора монстра, а не только кампания.
 */
enum class EnumMonsterRarity { NORMAL, MAGIC, RARE, UNIQUE }
